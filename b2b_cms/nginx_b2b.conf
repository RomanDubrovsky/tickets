# --- b2b_cms: Акватория Звука (порт 8081) ---
server {
    listen 8081;
    server_name _;

    # Статические ассеты шаблона Spectral
    location /static/ {
        alias /home/ubuntu/b2b_cms/static_assets/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    root /home/ubuntu/b2b_cms/dist/aquasound.club;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# --- b2b_cms: Рок Хит Нева (порт 8082) ---
server {
    listen 8082;
    server_name _;

    location /static/ {
        alias /home/ubuntu/b2b_cms/static_assets/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    root /home/ubuntu/b2b_cms/dist/rockhitneva.ru;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# --- b2b_cms: Админка CMS ---
server {
    listen 8083;
    server_name _;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
